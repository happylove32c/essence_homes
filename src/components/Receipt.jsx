import React from 'react'

const Receipt = () => {
  return (
    <div>
        {/* => table of things bought, items, prices and total,
         => with instructions to be screenshot,
         => and qrcode to confirm delivery. */}
    </div>
  )
}

export default Receipt